class BookRepository {
    constructor(db) {
        this.db = db;
    }

    changeBooks(books = []) {
        //todo change books
    }

    getBooks() {
        //todo
    }
}

module.exports = BookRepository;
